{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Build and Debug",
      "type": "PowerShell",
      "request": "launch",
      "script": "${cwd}/build.ps1",
      "args": [
        "-Task Test",
        "-Verbose"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}
